import 'package:flutter/material.dart';

const primaryColor = Color(0xFFF28930);
final informationColor = Color.lerp(primaryColor, Colors.white, 0.6)!;
const signColor = Color(0xFFFE751A);
const informationText = Color(0xFF989191);
const backgroundColor = Color(0xFFFFFFFF);